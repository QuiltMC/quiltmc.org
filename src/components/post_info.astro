---
const {post} = Astro.props;
---

<div class="media">
    <div class="media-content">
        {(post.author || post.authors) &&
            <p class="title is-5">{post.author || post.authors.join(", ")}</p>}

        <p class="subtitle is-6">{new Date(post.date).toLocaleString("en-GB", {
            year: "numeric",
            month: "long",
            day: "numeric"
        })}</p>
    </div>
</div>