---
import TeamMember from '@parts/team/TeamMember.astro';
import teamData from '@data/Team';

export interface Props {
	list: string[];
}

const { list } = Astro.props as Props;
---

{list.map((name) =>
	<TeamMember member={teamData[name]} />
)}
